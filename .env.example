# Database Configuration
DB_CONNECTION_STRING=postgres://kanban_user:kanban_password@postgres:5432/kanban_db

# Alternative discrete database configuration
DB_NAME=kanban_db
DB_USER=kanban_user
DB_PASSWORD=kanban_password
DB_HOST=postgres

# JWT Configuration - CHANGE THESE IN PRODUCTION!
JWT_SECRET_KEY=your_super_secret_jwt_key_change_this_in_production
ACCESS_TOKEN_SECRET=your_super_secret_access_token_change_this_in_production

# Application Configuration
NODE_ENV=production
PORT=3001

# PostgreSQL Configuration (for docker-compose)
POSTGRES_DB=kanban_db
POSTGRES_USER=kanban_user
POSTGRES_PASSWORD=kanban_password

# pgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@kanban.local
PGADMIN_DEFAULT_PASSWORD=admin