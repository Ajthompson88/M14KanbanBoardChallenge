# Server on Render
NODE_ENV=production
# Render provides PORT at runtime; your code should read process.env.PORT (you can still set a default)
# PORT=10000  # (optional explicit default; Render injects its own)

# Use Render’s **Internal Connection String** from your Render Postgres
DB_URL=postgresql://<user>:<password>@<your-db-host>.render.internal:5432/<dbname>

# Set *real* secrets here
JWT_SECRET_KEY=change-me-in-render
ACCESS_TOKEN_SECRET=change-me-in-render
